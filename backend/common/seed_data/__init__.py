"""
seed_demo が使う文章の素材。

言語ごとに 1 モジュール（ja / en）を置き、どちらも同じ名前の変数を持たせてある。
seed_demo は「日本語圏の人」「英語圏の人」を作るときに、この 2 つを同じように扱う。

文章は完成品ではなく素材。短い文をいくつも用意しておき、seed_demo 側で 1〜3 文を
組み合わせて 1 件の投稿にする。こうすると数百の素材から数万通りの本文が作れるので、
一覧を眺めたときに同じ文章が並ばずに済む。
"""

from . import en, ja

# 日本語圏・英語圏の 2 言語で作る。増やすときはここに足す。
LOCALES = [ja, en]

__all__ = ["LOCALES", "en", "ja"]
