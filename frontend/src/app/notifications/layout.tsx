import { routeMetadata } from "../routeMetadata";

export const metadata = routeMetadata.notifications;

export default function NotificationsLayout({ children }: { children: React.ReactNode }) {
  return children;
}
