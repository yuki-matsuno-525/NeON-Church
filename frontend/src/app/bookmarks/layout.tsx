import { routeMetadata } from "../routeMetadata";

export const metadata = routeMetadata.bookmarks;

export default function BookmarksLayout({ children }: { children: React.ReactNode }) {
  return children;
}
