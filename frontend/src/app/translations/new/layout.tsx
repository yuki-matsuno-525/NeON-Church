import { routeMetadata } from "../../routeMetadata";

export const metadata = routeMetadata.newTranslation;

export default function NewTranslationLayout({ children }: { children: React.ReactNode }) {
  return children;
}
