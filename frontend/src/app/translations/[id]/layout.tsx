import { routeMetadata } from "../../routeMetadata";

export const metadata = routeMetadata.translationProject;

export default function TranslationProjectLayout({ children }: { children: React.ReactNode }) {
  return children;
}
