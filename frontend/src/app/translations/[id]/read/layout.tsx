import { routeMetadata } from "../../../routeMetadata";

export const metadata = routeMetadata.translationReader;

export default function TranslationReaderLayout({ children }: { children: React.ReactNode }) {
  return children;
}
