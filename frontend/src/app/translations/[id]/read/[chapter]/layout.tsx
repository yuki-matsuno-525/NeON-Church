import { routeMetadata } from "../../../../routeMetadata";

export const metadata = routeMetadata.translationChapter;

export default function TranslationChapterLayout({ children }: { children: React.ReactNode }) {
  return children;
}
