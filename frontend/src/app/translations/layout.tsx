import { routeMetadata } from "../routeMetadata";

export const metadata = routeMetadata.translations;

export default function TranslationsLayout({ children }: { children: React.ReactNode }) {
  return children;
}
