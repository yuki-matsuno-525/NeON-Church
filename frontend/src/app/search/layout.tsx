import { routeMetadata } from "../routeMetadata";

export const metadata = routeMetadata.search;

export default function SearchLayout({ children }: { children: React.ReactNode }) {
  return children;
}
