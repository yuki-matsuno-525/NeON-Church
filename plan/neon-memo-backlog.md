# NeON メモ整理・進捗管理

ユーザーメモ（2026-07-13）を、種類ごとに整理した作業バックログ。
各項目は独立して着手・push できる単位に分ける。着手前にこのファイルの状態列を更新する。

**状態の凡例**：`未着手` / `調査中` / `着手中` / `レビュー待ち` / `完了`
**優先度**：`P0=バグ（体験を壊す）` / `P1=分かりやすさ・情報設計` / `P2=磨き込み`

---

## A. バグ修正（P0）

| ID | 状態 | 内容 |
|----|------|------|
| A-1 | → C-3 に統合 | **翻訳プロジェクトのコメント欄からQ&A投稿ができない**。原因＝翻訳プロジェクトにコメント系統が2つある。①プロジェクト詳細ページ(`/translations/[id]`)は `TranslationComment`（`postUnitComment`）で **body のみ・Q&A非対応**の翻訳作業レビュー用。②読書ページ(`/translations/[id]/read/[chapter]`)は通常 `Comment`＋Q&A対応で、**HTTPで投稿成功を確認済み**。クラッシュではなく設計ギャップ。**方針決定: 2系統の統一を含め C-3（翻訳PJ作り込み）で腰を据えて設計する。単独では着手しない。** |
| A-2 | 完了（PR #33） | **プロフィールのコメント**。データは機能していた（`/comments/mine/` 30件）。実体は **カードにリンクが無く該当箇所へ飛べない**こと。→ `MyCommentSerializer` に `book_slug`/`章`/`節`/`訳` を追加し、`CommentList` を該当節へのリンク（`/{slug}/{章}?translation={訳}#verse-{節}`）に変更。**追補**: 箇所ラベルを投稿時訳の書名（ギリシャ語名など）ではなく **表示言語の書名**で出すよう `formatBookLocation` を導入。※HMRが効かない環境のため、確認時はフロント再起動が必要 |
| A-3 | 完了 | **今日の聖句が en と ja で違う**。原因＝`verse-of-the-day` が書の `order`（訳ごとにインポート順で振られズレる）で対応節を引いていた。→ 訳非依存の `canonical_book` で引くよう修正。回帰テスト追加（4 passed）。※ローカルDBは全訳を ibibles で入れ直し order を揃えた |

---

## B. プロフィール（P1）

| ID | 状態 | 内容 |
|----|------|------|
| B-1 | 完了（PR #33） | **お気に入り（栞）に書・章・節を表示＋箇所へリンク**。① 節の栞：リンクを `#verse-{節}` まで飛ぶよう変更し、**節本文を表示**（訳非依存の箇所しか持たないため、一覧 view で Subquery により節本文を annotate＝口語訳優先・N+1回避）。② コメントの栞：`CommentBriefSerializer` に箇所情報を追加し、箇所ラベル（表示言語の書名）＋本文＋その節へリンク。自分/公開の両プロフィールに適用。共通ヘルパー `frontend/src/lib/passage.ts`（`passageHref`）を新設し A-2 と共用 |
| B-2 | 未着手（要デザイン方針） | **プロフィールのパネルをクリアに**整理するか検討。情報の取捨・見た目は主観が入るため、**G-1（全体デザインレビュー）と方針をそろえてから**着手するのが手戻り少ない |

---

## C. 翻訳プロジェクト（P1〜P2）

| ID | 状態 | 内容 |
|----|------|------|
| C-1 | 完了（PR #34） | **翻訳プロジェクトパネルの絵文字（本・地球）を削除**。一覧カードの 📖 / 🌐 を除去（書名・言語はテキストのまま） |
| C-2 | 未着手 | **Q&Aから翻訳プロジェクトを選べるようにする**（A-1のバグと関連。フィルタ／投稿先に翻訳プロジェクトを含める） |
| C-3 | 未着手 | **翻訳プロジェクト全体の作り込み**（大きめ）。安っぽさの解消。例：レビュー対象からユニット（対象箇所）へ飛べる導線。要別途設計 → 着手時に専用planを作る |

---

## D. Q&A（P1）

| ID | 状態 | 内容 |
|----|------|------|
| D-1 | 完了（PR #34） | **Q&Aの「すべて / 未解決 / 解決済み」テキストを削除**。フィルタボタンと重複する見出しを画面から隠す（`sr-only` でスクリーンリーダー用には残す） |
| D-2 | 未着手 | **タグ項目の再考**（Q&Aタグの粒度・種類を見直す）。要方針検討 |

---

## E. 検索（P1）

| ID | 状態 | 内容 |
|----|------|------|
| E-1 | プラン作成済み | **検索の作り直し**。調査結果＝コードは全書・全訳を検索できており「福音書のみ」は**本番のデータ未投入**の疑い。決定版プランは `plan/search-rebuild.md`（段階0=本番データ確定／1=訳の重複解消・言語スコープ／2=フィルタ・ページング／3=pg_trgm索引で高速化／4=磨き込み） |

---

## F. 書一覧・情報設計（P1）

| ID | 状態 | 内容 |
|----|------|------|
| F-1 | 完了（PR #35） | **書一覧のカテゴリに「翻訳本棚」も含める**。最下部の常設セクションを廃し、ジャンルと並ぶカテゴリチップ「本棚」に統合（本棚に公開翻訳が1件以上あるときだけ表示） |

---

## G. デザイン全般（P2）

| ID | 状態 | 内容 |
|----|------|------|
| G-1 | 未着手 | **全体のデザインレビュー**。主要画面のスクショを撮り、外部（ChatGPT等）にも見てもらって改善点を洗い出す |

---

## 進め方メモ

- まず **A（バグ）** を優先。A-1 と C-2 は関連するので一緒に調査すると効率的。
- **C-3 / E-1** は範囲が大きいため、着手時にそれぞれ専用の plan ファイルを切る。
- デザイン方針が絡む項目（B-2, C-1, C-3, D-1）は、G-1 のレビュー結果と方針をそろえてから着手すると手戻りが少ない。
